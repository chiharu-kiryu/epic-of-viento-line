# 项目类型与模板修复 · b.3.1 工作树

2026-09-23。接续功能图修订 20，检查 F16、F23–F26 的“新增类型 → 保存模板 → 新建文档 → 再次打开设置”链路。Git 基线为 b.3.1 发布提交 `759fe3c`；保留上一轮素材引用修复，本轮不增加版本或提交、安装。

## 修复

| 编号 | 原有问题 | 修复后的行为 |
| --- | --- | --- |
| N62 | 在“新增类型”中填写已有类型标识，服务会当作编辑操作，覆盖旧类型的名称、规则和模板。 | 表单明确传递新增意图；服务在登记锁内拒绝重复标识，返回 409，清单和模板文件均不改写。窗口保留草稿，可以换标识重试或取消。编辑已有类型仍可正常保存，未传新增标记的旧接口调用保持兼容。 |
| N63 | 模板设置接受隐藏目录、`node_modules` 和超过 255 字节的目录名，但新建文档拒绝这些位置，导致设置保存成功后无法使用默认路径。 | 预览、保存与新建文档共用路径校验，问题在写入配置前报告。旧清单仍能读取，设置显示问题并允许修正；合法多层中文目录和 255 字节目录名可以保存并创建文档。 |

模板变更仍只修改项目清单及模板。既有原文、文档 UUID、登记和归属不改写；重复新增被拒绝后，旧规则及模板原文保持原样。空子目录仍代表正文根目录。

## 验证

- 新增 **8 个执行场景**。修复前 7 个复现问题、1 个兼容边界通过；17 个旧场景因名称筛选未运行。
- 类型引擎与编辑器弹窗专项：**25 项全部通过**。覆盖真实 HTTP 重复新增拒绝、正常编辑保存、模板重读、无效默认路径拒绝、旧配置修复、255 字节目录中新建，以及表单失败后保留、重试和取消。
- 完整应用检查：**243 项，242 通过、0 失败、1 跳过**；120 份 JavaScript 语法、API 契约和 b.3.1 版本一致性检查通过。
- 专项使用 Node v18.19.1，全量使用 Node v24.19.0。跳过项需要 `VIENTO_TEST_ARCHIVE_BINARY`；原生归档实现未改动，本轮未重跑 Rust、桌面窗口或归档互通测试。

验证全部使用临时作品；未读取或修改日常作品。原始日志及五份修改源码指纹见 [验证记录](test-results/project-settings-2026-09-23/results.json)。功能图更新为修订 21，保留此前发布与排查证据。
