# 模板网络读取与新建文档修复 · b.3.6 工作树

2026-09-23，接续第二十六轮 N88，检查 F16、F19：项目模板 → 网络读取 → 新建草稿 → 保存、登记和重建 → 再次打开原文。Git 基线仍为 `8315e0b`，上一轮修复保留。

## 修复

| 编号 | 原有问题 | 修复后的行为 |
| --- | --- | --- |
| N89 | 模板设置能正确读取包含 `#`、`%` 的合法文件名，但新建时直接把文件路径当作网址。片段标记截断路径，百分号触发错误解码；可能报 400/404，也可能悄悄读取另一个文件。 | 请求前分别编码路径的每个组成部分，保留目录分隔符，按清单中的原始文件名读取。字面的 `%23`、`%2f` 也不会变成其他文件或目录。 |
| N90 | 网络文本读取默认移除 UTF-8 BOM，新建草稿及随后保存的文件因而缺少模板原有的编码标记。 | 在原有请求超时范围内读取字节并保留 BOM；原文换行继续保留，错误响应和 HTML 回退仍正确拒绝。 |

## 验证

新增 **19 个执行场景**；修复前 **15 失败、4 个兼容场景通过**，修复后全部通过：

- v2、v3 项目分别检查文件名和目录名中的 `#`、普通 `%`、字面 `%23` 和 `%2f`，以及普通中文、空格、表情名称。放置可被误读的相近文件，核对新建使用的确实是配置模板，并确认相近文件未改写。
- Markdown、TXT、JSON、YAML、YML 均通过实际网络读取创建文档，逐字节核对 BOM 和 CRLF，再执行登记、重建、读取与重新进入编辑。新建位置可以不同于类型默认目录；文档保留选定类型、稳定身份和正确内容版本，索引布局与模板预览相同。
- 模板缺失后恢复为空文件可以重试并保存；HTML 页面仍被拒绝，不能作为新文档原文。
- 项目清单、原模板和已有正文保持原字节，已有文档登记保持原样。

测试连接实际编辑器控制器、请求模块和临时作品的本地 HTTP 服务；测试适配层只为浏览器相对网址补上测试服务地址，页面排版仍由 DOM 测试环境替代。本轮没有原生窗口操作或媒体解码测试。

专项 **120 项全部通过**，包括既有模板切换、取消、迟到响应、请求体超时和草稿重试回归。完整应用检查 **336 项：335 通过、0 失败、1 跳过**；126 份 JavaScript 语法、API 契约及 b.3.6 版本一致性检查通过。专项使用 Node v18.19.1，全量使用 Node v24.19.0。

原始失败、回归日志和源码指纹见 [验证记录](test-results/template-http-2026-09-23/results.json)。功能图更新为修订 37，保留历次排查与发布证据。原生归档互通项因未设置 `VIENTO_TEST_ARCHIVE_BINARY` 跳过；本轮未重跑 Rust。未增加版本、提交或安装，日常作品未动。
