# 图片地址与缺图回退修复 · b.3.6 工作树

2026-09-23，接续第二十七轮 N89–N90，检查 F12、F13：登记的素材路径 → 列表缩略图、封面、图库和旧技能卡片 → 静态资源请求 → 缺图回退。Git 基线仍为 `8315e0b`，此前未提交的修复保留。

## 修复

| 编号 | 原有问题 | 修复后的行为 |
| --- | --- | --- |
| N91 | 封面和旧技能卡片直接把素材文件名作为网址，未沿用图库的编码规则。`#` 会截断请求，普通 `%` 或字面的 `%20`、`%2f` 可能导致请求失败或读取相近文件；占位图片也受影响。 | 统一使用现有图片地址和回退函数，逐段编码原始文件名。封面、缩略图、图库、技能图及占位文件读取一致；保留原有替代文字与占位样式。 |
| N92 | 图片解析器拒绝任何含 `..` 的路径，导致 `原画..定稿.png` 或 `草案..定稿/原画.png` 等合法名称在列表和图库中丢图。 | 按完整路径段检查 `.`、`..`，允许文件名或目录名内部的连续点；父目录跳转、反斜杠和不受支持的来源继续拒绝。 |
| N93 | 通用文档有封面引用，但图片不存在且没有旧英雄占位路径时，封面没有错误回退处理，留下破图。 | 图片加载失败后显示名称头像；全部候选失效后停止重试。 |

## 验证

新增 **20 个执行场景**，修复前 **17 失败、3 个兼容场景通过**，修复后全部通过：

- 封面和旧技能图分别检查 `#`、普通 `%`、字面的 `%20`、`%2f`、连续点及中文、空格、全角字母、表情名称。放置相近文件，核对真正读到的文件内容，并对照图库与列表缩略图。
- 合法文件名和目录名中的连续点都能经过实际 HTTP 服务读取；父目录跳转、反斜杠、外部网址和不受支持的协议继续被拒绝。
- 封面、技能图分别覆盖主图缺失和未配置主图，使用含 `#`、`%` 的旧占位路径，最终回退到名称头像，重复错误不会继续切换或重试。
- 通用文档缺少旧英雄占位路径时也有名称头像。临时作品的原文 BOM/CRLF、素材文件内容和登记记录在验证后保持原样。

使用实际渲染函数、编辑器控制器和本地 HTTP 服务；DOM 由测试环境提供，适配层仅替换测试服务端口，不改写渲染器生成的请求路径。检查的是图片地址和实际读到的文件内容；未进行图片解码、原生窗口操作或视觉排版验证。

专项检查 **102 项：101 通过、0 失败、1 跳过**，覆盖媒体、结构化渲染、素材分离、列表筛选及导出回归。完整应用检查 **356 项：355 通过、0 失败、1 跳过**；127 份 JavaScript 语法、API 契约及 b.3.6 版本一致性检查通过。专项使用 Node v18.19.1，全量使用 Node v24.19.0。跳过项均为未设置 `VIENTO_TEST_ARCHIVE_BINARY` 的原生归档互通检查；本轮未重跑 Rust。

原始失败、回归日志和源码指纹见 [验证记录](test-results/image-display-2026-09-23/results.json)。功能图更新为修订 38，保留历次排查和发布证据。未增加版本、提交或安装，日常作品未动。
