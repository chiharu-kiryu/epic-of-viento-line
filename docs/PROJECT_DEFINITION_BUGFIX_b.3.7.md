# 项目定义采用与模板校验修复 · b.3.7 工作树

2026-09-23，接续第二十九轮，基于 b.3.7 发布提交 `ed86c84` 检查 F43：预览项目定义 → 应用类型与模板规则 → 新建、重建及导出，并覆盖 F19 的新建保存接口。全部使用临时作品，日常作品未读取或修改。

## 修复

| 编号 | 原有问题 | 修复后的行为 |
| --- | --- | --- |
| N97 | 采用定义时直接跳过没有独立模板文件的类型，预览与实际应用均接受隐藏目录、`node_modules` 或超过 255 字节的目录名；保存的类型无法在默认位置新建文档。 | 自动生成的 Markdown 起始正文也经过共享模板预览，统一检查默认位置和解析规则，失败时不发布配置或日志。旧配置仍能读取并修正，v2、v3 与官方示范定义继续兼容。 |
| N98 | 恢复日志只用毫秒时间命名；同一毫秒再次应用，或清单写入失败后立即重试，会因已有日志而报 `EEXIST`。 | 日志名增加独立 UUID，继续禁止覆盖；连续变更和失败后的重试均保留各自记录，旧日志内容不变。 |

## 验证

新增 **10 个执行场景**，修复前 **7 失败、3 个兼容场景通过**，修复后全部通过：

- 五种不可用目录分别通过实际维护命令检查预览和 `--write`；两者均拒绝，清单及原文、素材、描述文件保持原字节，不生成恢复日志。
- v2、v3 保留旧的无效目录可读性，通过共享定义修正为含中文的合法 255 字节目录段。预览不改清单，实际应用保存前后配置，重复应用不新增日志。
- 通过真实 HTTP 接口读取自动生成模板、按类型新建、重开及重建索引，核对标题和字段分组，再实际生成 HTML 分享内容。既有文档、BOM/CRLF、模板、素材、UUID、背景归属备注与附件绑定逐字节保留。
- 缺失、无法解析及超过 1 MB 的模板在写入前拒绝；既有官方示范采用测试及模板管理、冲突检查继续通过。
- 在隔离子进程固定时钟，连续两次应用保留独立日志，第二次以前一次配置为基线。在清单原子替换处注入失败，核对原清单、登记锁释放、立即重试成功及失败时日志保留。

专项 **81 项：80 通过、0 失败、1 跳过**，覆盖项目定义、类型模板、配置快照、模板网络读取、上一轮归属迁移和导出。完整应用检查 **375 项：374 通过、0 失败、1 跳过**；129 份 JavaScript 语法、API 契约及 b.3.7 版本一致性检查通过。专项使用 Node v18.19.1，全量使用 Node v24.19.0。跳过项是未设置 `VIENTO_TEST_ARCHIVE_BINARY` 的原生归档互通检查。

原始日志、命令与源码指纹见 [验证记录](test-results/project-definition-2026-09-23/results.json)。功能图更新为修订 41，并补充[通用项目说明](GENERIC_PROJECTS.md)。本轮运行真实维护命令与 HTTP 接口；没有操作原生窗口，Rust 与媒体解码未重跑。版本仍为 b.3.7，尚未提交或安装。
