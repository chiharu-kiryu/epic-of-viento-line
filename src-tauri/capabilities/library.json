{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "library",
  "description": "The packaged library window can observe its own application events. Editor web content receives no native capability.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
